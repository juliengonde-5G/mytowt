# Rapport Quotidien my_TOWT
**Date**: 2026-03-30
**Auteur**: Chef de Projet (audit automatise)
**Version applicative**: 2.0.0
**Rapport precedent**: rapport_2026-03-29.md

---

## 1. CONTROLE DES BRANCHES

### Etat du repository
| Element | Valeur |
|---------|--------|
| Branche principale | `main` (origin) / `master` (local) |
| Commit HEAD | `10c8a28` |
| Commits sur main (total) | 91 |
| Working tree | Clean |

### Synchronisation effectuee
9 commits etaient en avance (detached HEAD) et ont ete merges (fast-forward) sur master :

| Commit | Description | Fichiers |
|--------|-------------|----------|
| `37a1779` | fix: remove duplicate local Claim import (KPI) | kpi_router.py |
| `fe633b4` | fix: KPI page NameError - totals -> env_totals | kpi_router.py |
| `f98b528` | fix: KPI modal + BL numbers unique per leg | kpi/index.html, cargo_router.py |
| `1e168b3` | fix: passenger_bookings.leg_id nullable | passenger.py |
| `5b6d9a0` | chore: daily audit report + backup script | rapports/, scripts/ |
| `0f13f54` | chore: update daily report | rapports/ |
| `61426de` | chore: daily audit report 2026-03-29 | rapports/ |
| `eb340af` | chore: add backups/ to .gitignore | .gitignore |
| `10c8a28` | chore: enrich daily report with deep QA | rapports/ |

**Statut** : `master` et `origin/main` synchronises a `10c8a28`.

### Branches distantes
| Branche | Statut | Commentaire |
|---------|--------|-------------|
| `origin/main` | A jour | Seule branche restante |

**Amelioration** : les 7 branches stales signalees le 29/03 ont ete nettoyees. Le repo ne contient plus qu'une seule branche distante.

---

## 2. AUDIT DE SECURITE

### 2.1 Suivi des correctifs (rapport J-1 et J-2)

| # | Issue | Statut | Commentaire |
|---|-------|--------|-------------|
| S1 | SECRET_KEY par defaut | **NON CORRIGE** | `config.py:17` — `towt_secret_key_change_in_production_2025` |
| S2 | Mot de passe DB par defaut | **NON CORRIGE** | `config.py:14` — `towt_secure_2025` en dur |
| S3 | Port PostgreSQL expose 0.0.0.0:5433 | **NON CORRIGE** | `docker-compose.yml:32` — `"5433:5432"` sans restriction IP |
| S4 | Uploads sans validation | **PARTIELLEMENT CORRIGE** | `file_validation.py` OK (magic bytes), mais non applique sur tous les routers |
| S5 | onboard_router sans permissions individuelles | **CORRIGE (router-level)** | `main.py:200` — `require_permission("captain", "C")` applique globalement |
| S6 | stowage sans auth globale | **PARTIELLEMENT CORRIGE** | Permissions per-endpoint OK (9 occurrences), mais pas de dependency globale en main.py |
| S7b | Path traversal claim_router | **NON CORRIGE** | `claim_router.py:51` — pas de `os.path.basename()` |
| S8 | Bare `except:` | **NON CORRIGE** | 13 occurrences dans 6 fichiers |
| S13 | f-string SQL admin export | **NON CORRIGE** | `admin_router.py:779,831` — protege par whitelist mais mauvaise pratique |

**Bilan correctifs** : 1 corrige, 2 partiels, **6 en souffrance dont 3 CRITIQUES (S1, S2, S3).**

### 2.2 Nouvelles constatations

#### CRITIQUE

| # | Probleme | Fichier | Lignes | Impact |
|---|----------|---------|--------|--------|
| S16 | **`await db.commit()` dans routes** | auth_router.py, passenger_ext_router.py, pricing_router.py, tracking_router.py | 83,93,120 / 328 / 893 / 203 | Commit premature dans les routes alors que `get_db()` gere auto-commit. Risque de donnees partiellement commitees non-rollbackables en cas d'erreur subsequente. |

#### MAJEUR

| # | Probleme | Fichier | Lignes | Impact |
|---|----------|---------|--------|--------|
| S17 | **tracking_router sans aucune auth** | tracking_router.py, main.py:209 | toutes | API ouverte (`/api/tracking/*`) sans authentification ni API key. N'importe qui peut injecter des positions GPS. |
| S18 | **planning_ext_router sans rate limiting** | planning_ext_router.py | 26-31 | Token planning partageable brute-forcable (pas de `check_token_rate_limit`). |
| S19 | **XSS via `|safe` dans templates** | portal_destination.html:28, grid_form.html:163-165 | 4 occ. | `{{ destination_info|safe }}` et injection JS via `{{ default_brackets|safe }}`. Utiliser `|tojson` pour le JS. |
| S20 | **Mot de passe min 4 caracteres (Mon Compte)** | admin_router.py | 1382 | Changement password accepte 4 chars vs 8 chars pour la creation. |

#### Points positifs confirmes
- CORS correctement restreint (my.towt.eu + IP VPS) — 2 origines seulement
- Security headers complets (CSP avec HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)
- CSRF middleware actif avec double-submit cookie pattern
- Cookies session : `httponly=True`, `secure=True`, `samesite=lax`
- Rate limiting login : 5 tentatives/60s, lockout 5 min
- Rate limiting portail cargo/passager : 10 tentatives/60s, lockout 5 min
- File validation correcte dans `file_validation.py` (magic bytes + MIME)
- Dependencies a jour, pas de CVE connue (fastapi 0.115.6, sqlalchemy 2.0.36, bcrypt 4.2.1)
- Tokens portail avec expiration + logging d'acces
- admin_router protege globalement par `require_admin` (main.py:191)
- Toutes les routes principales protegees au niveau routeur (13 routers avec dependencies)

---

## 3. TESTS FONCTIONNELS PAR ROLE

### 3.1 Commandant de bord (role: marins / module: captain)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F1 | CORRIGE | — | Permissions onboard : desormais protege par `require_permission("captain", "C")` au niveau routeur (main.py:200). | main.py:200 |
| F2 | BUG | MAJEUR | **Port timezone resolution defaillante** : `db.get(Port, leg.departure_port_id)` retourne toujours None car le champ est `departure_port_locode` (string), pas `departure_port_id` (int). Timezone defaut UTC. | onboard_router.py:264-271 |
| F3 | PERF | MAJEUR | **N+1 queries** dans `sof_add_event()` : le meme Leg est charge 3 fois dans la meme requete (lignes 264, 294, 307). | onboard_router.py:264,294,307 |
| F19 | LOGIQUE | MAJEUR | **ETAShift + resequence_and_recalc** appele AVANT flush. Si la recalculation echoue, etat inconsistant. Pas de savepoint. | onboard_router.py:541-670 |
| F20 | DATA | MAJEUR | **ETA shift cascade ecrase les ajustements manuels** des legs downstream. Seul le leg edite est preserve. | planning_router.py:119-184 |

### 3.2 Responsable Crew (role: armement / module: crew)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F5 | BUG | MAJEUR | `except:` nu dans `parse_date()` — masque erreurs systeme. | crew_router.py:34 |
| F6 | DATA | MAJEUR | Pas de controle de **chevauchement d'affectations** : marin affectable a 2 navires simultanement. | crew_router.py:299+ |
| F7 | DATA | MINEUR | `embark_leg_id` / `disembark_leg_id` jamais remplis par le formulaire — champs orphelins. | crew_router.py |
| F28 | DATA | MAJEUR | Pas de validation `embark_date <= disembark_date` lors de la creation d'assignment. | crew_router.py:290-315 |
| F29 | LOGIQUE | MOYEN | **Status CrewAssignment inversee** : `"active"` si pas de disembark_date (correct), mais `"planned"` si disembark_date fournie (logique inversee — devrait etre base sur les dates vs today). | crew_router.py:304 |

### 3.3 Responsable Operations (role: operation / module: escale + planning)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F9 | LOGIQUE | CORRIGE | `resequence_and_recalc()` : logique de cascade downstream correcte. | planning_router.py:119-184 |
| F10 | DATA | MAJEUR | `port_stay_days` fallback a 3 jours si NULL en code. Incoherence si valeur mise a NULL directement en base. | escale_router.py:214 |
| F21 | DATA | BLOQUANT | **Pas de validation `ETA >= ETD`** en creation de leg. Possible de creer un leg avec ETA < ETD. | planning_router.py:620-623 |
| F22 | DATA | MAJEUR | Pas de verification `departure_port != arrival_port`. Legs avec distance_nm=0 possibles. | planning_router.py:569-574 |
| F23 | LOGIQUE | BLOQUANT | `resequence_and_recalc()` sans savepoint/rollback. Si cascade echoue, etat partiellement modifie. | planning_router.py |
| F30 | LOGIQUE | MAJEUR | **Stowage : validation marchandises dangereuses contournable** si `batch.imo_product_class` est vide. Le flag `is_dangerous` est mis a 0 meme si l'ordre contient du IMDG. | stowage_router.py:287-303 |
| F31 | CODE | MINEUR | **Stowage : `get_zone_capacity()` retourne None** si format palette inconnu. Division par None avant le check de capacite. | stowage_router.py:305-320 |

### 3.4 Responsable Commercial (role: commercial / module: commercial + cargo)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F13 | DATA | BLOQUANT | **Dualite Order.leg_id + OrderAssignment** non resolue. Double source de verite. | order.py:76-109 |
| F14 | LOGIQUE | MAJEUR | `find_matching_leg()` retourne le premier match sans disambiguation si multiples navires. | commercial_router.py:63 |
| F15 | DATA | MAJEUR | Suppression Order ne cascade pas suppression PackingList. Donnees orphelines. | order.py, packing_list.py |
| F16 | UX | MAJEUR | Reservations passagers "draft" verrouillent cabines indefiniment (pas d'expiration). | passenger_router.py:184 |
| F25 | DATA | BLOQUANT | `create_packing_list()` ne valide pas que `order.leg_id` est non NULL. Packing lists orphelines. | cargo_router.py:98-125 |
| F32 | DATA | MAJEUR | **Race condition reference Order** : `generate_reference()` compte les ordres existants. Deux ordres simultanes generent la meme reference. | commercial_router.py:53-60 |
| F33 | DATA | MAJEUR | **Race condition cabine passager** : deux reservations simultanees pour la meme cabine passent le check `existing_cabin`. | passenger_router.py:184-193 |
| F34 | LOGIQUE | MINEUR | Pipedrive sync : `except: pass` silencieux. Si API fail, deal non cree sans log ni alerte. | commercial_router.py:286-304 |

---

## 4. SAUVEGARDE BASE DE DONNEES

| Element | Valeur |
|---------|--------|
| Script | `scripts/backup_db.sh` (present et fonctionnel) |
| Execution | **ECHEC** — Docker non disponible dans cet environnement |
| Erreur | `failed to connect to docker API at unix:///var/run/docker.sock` |
| Format | pg_dump compresse (gzip) |
| Rotation | 30 jours (configurable via `BACKUP_RETENTION_DAYS`) |

**Action requise** : verifier que le cron est configure sur le VPS de production :
```
0 2 * * * /home/ubuntu/mytowt/scripts/backup_db.sh >> /var/log/towt-backup.log 2>&1
```

---

## 5. DOCUMENTATION

### Aucune mise a jour necessaire aujourd'hui
- CLAUDE.md a ete mis a jour le 29/03 avec les 5 modeles manquants
- Structure du projet conforme a la realite du code
- 25 modeles, 20 routers actifs, 91 templates

### Statistiques du projet
| Metrique | Valeur | Delta J-1 |
|----------|--------|-----------|
| Routers | 20 actifs | = |
| Modeles | 25 | = |
| Templates HTML | 91 | = |
| Lignes de code (routers) | 16 559 | = |
| Lignes de code (modeles) | 2 747 | = |
| CSS (app.css) | 1 020 lignes | = |
| Commits sur main | 91 | = |
| Branches distantes | 1 | -7 (nettoyage) |

---

## 6. SYNTHESE ET PRIORITES

### Bilan securite global

| Categorie | Critique | Majeur | Moyen | Mineur |
|-----------|----------|--------|-------|--------|
| Secrets/Config | 2 (S1, S2) | 0 | 0 | 0 |
| Infra | 1 (S3) | 0 | 0 | 0 |
| Auth/Permissions | 0 | 1 (S17) | 0 | 0 |
| Injection SQL | 0 | 0 | 1 (S13) | 0 |
| XSS | 0 | 1 (S19) | 0 | 0 |
| Transaction | 1 (S16) | 0 | 0 | 0 |
| Rate limiting | 0 | 1 (S18) | 0 | 0 |
| Passwords | 0 | 1 (S20) | 0 | 0 |
| Code quality | 0 | 0 | 1 (S8) | 0 |
| Path traversal | 0 | 0 | 1 (S7b) | 0 |
| **Total** | **4** | **4** | **3** | **0** |

### Bilan bugs fonctionnels

| Role | Bloquant | Majeur | Mineur |
|------|----------|--------|--------|
| Commandant (captain) | 0 | 4 (F2,F3,F19,F20) | 0 |
| Resp Crew (armement) | 0 | 3 (F5,F6,F28) | 2 (F7,F29) |
| Resp Operations | 2 (F21,F23) | 3 (F10,F22,F30) | 1 (F31) |
| Resp Commercial | 2 (F13,F25) | 5 (F14,F15,F16,F32,F33) | 1 (F34) |
| **Total** | **4** | **15** | **4** |

### Actions immediates (P0 — a faire aujourd'hui)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A1 | Supprimer `await db.commit()` des 4 routers (auth, passenger_ext, pricing, tracking) | Dev | S16 |
| A2 | Ajouter validation `ETA >= ETD` dans creation/edition de leg | Dev | F21 |
| A3 | Ajouter savepoint dans `resequence_and_recalc()` | Dev | F23 |
| A4 | Valider `order.leg_id is not None` avant creation packing list | Dev | F25 |

### Actions a planifier (P1 — cette semaine)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A5 | Verifier `.env` production : SECRET_KEY unique, DB password fort | DevOps | S1, S2 |
| A6 | Restreindre PostgreSQL a `127.0.0.1:5433:5432` | DevOps | S3 |
| A7 | Ajouter API key ou auth basique au tracking_router | Dev | S17 |
| A8 | Ajouter rate limiting au planning_ext_router | Dev | S18 |
| A9 | Remplacer `|safe` par `|tojson` pour les injections JS | Dev | S19 |
| A10 | Unifier minimum password a 8 chars dans `/my-account/password` | Dev | S20 |
| A11 | Corriger port timezone resolution (utiliser `departure_port_locode`) | Dev | F2 |
| A12 | Resoudre dualite Order.leg_id / OrderAssignment | Architecte | F13 |
| A13 | Ajouter controle chevauchement affectations crew | Dev | F6 |
| A14 | Corriger path traversal claim_router : `os.path.basename()` | Dev | S7b |
| A15 | Remplacer 13 `except:` nus par `except Exception:` | Dev | S8 |
| A16 | Ajouter validation `departure_port != arrival_port` | Dev | F22 |
| A17 | Ajouter validation `embark_date <= disembark_date` pour crew | Dev | F28 |

### Actions backlog (P2)

| # | Action | Ref |
|---|--------|-----|
| A18 | Migrer vers Alembic pour les migrations schema | Architecture |
| A19 | Logging structure (structlog) | Qualite |
| A20 | Supprimer `unsafe-inline` du CSP (nonces) | Securite |
| A21 | Tests automatises (pytest + httpx) | Qualite |
| A22 | Rate limiting Redis (multi-instance) | Infra |
| A23 | Cascade suppression Order -> PackingList | F15 |
| A24 | Expiration automatique reservations "draft" | F16 |
| A25 | Database sequence pour references Order (anti race condition) | F32 |
| A26 | SELECT FOR UPDATE pour reservation cabine | F33 |

---

## 7. EVOLUTION DEPUIS J-1

| Indicateur | J-1 (29/03) | J (30/03) | Tendance |
|------------|-------------|-----------|----------|
| Commits sur main | 91 | 91 | = |
| Vulnerabilites CRITIQUES | 5 | 4 | -1 (S5 corrige) |
| Vulnerabilites MAJEURES | 4 | 4 | = (S17,S18,S19,S20 nouveaux, S5 sorti) |
| Bugs BLOQUANTS | 6 | 4 | -2 (F1 corrige, F19 requalifie MAJEUR) |
| Bugs MAJEURS | 12 | 15 | +3 (F29,F30,F31,F32,F33 nouveaux) |
| Branches stales | 7 | 0 | -7 (nettoyees) |
| Correctifs appliques (securite) | 1/10 | 2/13 | +1 (S5) |
| Correctifs appliques (fonctionnels) | 1/27 | 2/23 | +1 (F1) |

### Progression globale
- **Branches** : nettoyees, repository propre avec une seule branche
- **Securite** : 1 issue critique resolue (S5 permissions onboard), 4 nouvelles issues identifiees (S16-S20)
- **Fonctionnel** : 2 bugs resolus (F1, F9), 5 nouveaux bugs identifies par audit approfondi (F29-F34)
- **Tendance** : le nombre de bugs identifies augmente grace a l'approfondissement des audits, mais la dette technique reste stable

**Prochaine action prioritaire** : corriger les 4 actions P0 (S16, F21, F23, F25) avant le prochain deploiement.

---

*Rapport genere le 2026-03-30. Prochain audit prevu : 2026-03-31.*
