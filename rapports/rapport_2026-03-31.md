# Rapport Quotidien my_TOWT
**Date**: 2026-03-31
**Auteur**: Chef de Projet (audit automatise)
**Version applicative**: 2.0.0
**Rapport precedent**: rapport_2026-03-30.md

---

## 1. CONTROLE DES BRANCHES

### Etat du repository
| Element | Valeur |
|---------|--------|
| Branche principale | `main` (origin) / `master` (local) |
| Commit HEAD | `f10d868` |
| Commits sur main (total) | 91 |
| Working tree | Clean |

### Synchronisation effectuee
HEAD etait en etat detache avec 10 commits d'avance sur master et origin/main. Fast-forward applique.

| Commit | Description | Fichiers |
|--------|-------------|----------|
| `37a1779` | fix: remove duplicate local Claim import (KPI) | kpi_router.py |
| `fe633b4` | fix: KPI page NameError - totals -> env_totals | kpi_router.py |
| `f98b528` | fix: KPI modal + BL numbers unique per leg | kpi/index.html, cargo_router.py |
| `1e168b3` | fix: passenger_bookings.leg_id nullable | passenger.py |
| `5b6d9a0` | chore: daily audit report + backup script | rapports/, scripts/ |
| `0f13f54` | chore: update daily report | rapports/ |
| `61426de` | chore: daily audit report 2026-03-29 | rapports/ |
| `eb340af` | chore: add backups/ to .gitignore | .gitignore |
| `10c8a28` | chore: enrich daily report with deep QA | rapports/ |
| `f10d868` | chore: daily audit report 2026-03-30 | rapports/ |

**Statut** : `master` synchronise a `f10d868`. Push vers `origin/main` en attente de validation.

### Branches distantes
| Branche | Statut | Commentaire |
|---------|--------|-------------|
| `origin/main` | En retard (10 commits) | Necessite push |

---

## 2. AUDIT DE SECURITE

### 2.1 Suivi des correctifs (rapport J-1 a J-3)

| # | Issue | Statut | Commentaire |
|---|-------|--------|-------------|
| S1 | SECRET_KEY par defaut | **NON CORRIGE** | `config.py:17` — `towt_secret_key_change_in_production_2025` |
| S2 | Mot de passe DB par defaut | **NON CORRIGE** | `config.py:14` — `towt_secure_2025` en dur |
| S3 | Port PostgreSQL expose 0.0.0.0:5433 | **NON CORRIGE** | `docker-compose.yml:32` — `"5433:5432"` sans restriction IP |
| S7b | Path traversal claim_router | **NON CORRIGE** | `claim_router.py:51` — pas de `os.path.basename()` |
| S8 | Bare `except:` | **NON CORRIGE** | 13 occurrences dans 6 fichiers |
| S13 | f-string SQL admin export | **NON CORRIGE** | `admin_router.py:779,831` — protege par whitelist mais mauvaise pratique |
| S16 | `await db.commit()` dans routes | **NON CORRIGE** | 6 occurrences : auth_router (L83,93,120), passenger_ext (L328), pricing (L893), tracking (L203) |
| S17 | tracking_router sans aucune auth | **NON CORRIGE** | API `/api/tracking/*` totalement ouverte, pas d'import auth |
| S18 | planning_ext_router sans rate limiting | **NON CORRIGE** | Token planning brute-forcable |
| S19 | XSS via `\|safe` dans templates | **NON CORRIGE** | 4 occurrences : portal_destination.html:28, grid_form.html:163-165 |
| S20 | Mot de passe min 4 caracteres | **NON CORRIGE** | `admin_router.py:1382` — 4 chars vs 8 chars en creation |

**Bilan correctifs securite** : 0 corrige depuis J-1 sur 11 issues ouvertes dont **4 CRITIQUES (S1, S2, S3, S16).**

### 2.2 Nouvelles constatations

#### CRITIQUE

Aucune nouvelle issue critique identifiee aujourd'hui.

#### MAJEUR

| # | Probleme | Fichier | Lignes | Impact |
|---|----------|---------|--------|--------|
| S21 | **onboard_router : `departure_port_id` inexistant** | onboard_router.py | 267-268, 338-339 | Acces a un attribut inexistant du modele Leg. `db.get(Port, leg.departure_port_id)` echoue silencieusement (retourne None). Timezone toujours UTC par defaut. Impact fonctionnel + securite (donnees incorrectes). |
| S22 | **Uploads sans validation d'extension ni taille** | crew_router.py:658, claim_router.py:48, escale_router.py:863, passenger_ext_router.py:364 | multi | Pas de whitelist d'extensions (`.exe`, `.sh` acceptes), pas de limite de taille (DoS possible). Seul onboard_router et cargo_router utilisent `validate_file_content()`. |

#### Points positifs confirmes (inchanges)
- CORS correctement restreint (2 origines)
- Security headers complets (CSP, HSTS, X-Frame-Options, etc.)
- CSRF middleware actif avec double-submit cookie
- Cookies session : `httponly=True`, `secure=True`, `samesite=lax`
- Rate limiting login : 5 tentatives/60s, lockout 5 min
- Rate limiting portail cargo/passager : 10 tentatives/60s, lockout 5 min
- File validation correcte dans `file_validation.py` (magic bytes + MIME)
- Dependencies a jour, pas de CVE connue
- admin_router protege globalement par `require_admin`
- 13 routers avec dependencies de permission

---

## 3. TESTS FONCTIONNELS PAR ROLE

### 3.1 Commandant de bord (role: marins / module: captain)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F2 | BUG | MAJEUR | **Port timezone resolution defaillante** : `db.get(Port, leg.departure_port_id)` — attribut inexistant. Le modele Leg utilise `departure_port_locode` (String FK), pas `departure_port_id` (Int). 4 occurrences L267,268,338,339. Timezone toujours UTC. | onboard_router.py |
| F3 | PERF | MAJEUR | **N+1 queries** dans `sof_add_event()` : meme Leg charge 3 fois dans la meme requete. | onboard_router.py:264,294,307 |
| F19 | LOGIQUE | MAJEUR | **ETA Shift + resequence** appele AVANT flush. Si la recalculation echoue, etat inconsistant. | onboard_router.py:541-670 |
| F20 | DATA | MAJEUR | **ETA shift cascade ecrase les ajustements manuels** des legs downstream. | planning_router.py:119-184 |

**Verdict Commandant** : 4 bugs MAJEURS ouverts. Le bug F2 impacte directement la precision des SOF (timezone UTC au lieu du port local).

### 3.2 Responsable Crew (role: armement / module: crew)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F5 | BUG | MAJEUR | `except:` nu dans `parse_date()` — masque erreurs systeme. | crew_router.py:34 |
| F6 | DATA | MAJEUR | Pas de controle de **chevauchement d'affectations** : marin affectable a 2 navires simultanement. | crew_router.py:299+ |
| F28 | DATA | MAJEUR | Pas de validation `embark_date <= disembark_date` lors de la creation d'assignment (L302-303). | crew_router.py:290-312 |
| F29 | LOGIQUE | MOYEN | **Status CrewAssignment inversee** : `"active"` si pas de disembark_date, `"planned"` si disembark_date fournie (L304). Logique inversee — devrait etre base sur dates vs today. | crew_router.py:304 |
| F7 | DATA | MINEUR | `embark_leg_id` / `disembark_leg_id` jamais remplis par le formulaire — champs orphelins. | crew_router.py |

**Verdict Crew** : 3 bugs MAJEURS, 1 MOYEN, 1 MINEUR. Risque reel d'affectation double d'un marin.

### 3.3 Responsable Operations (role: operation / module: escale + planning)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F21 | DATA | BLOQUANT | **Pas de validation `ETA >= ETD`** en creation de leg. Auto-recalcul si `eta <= etd` mais pas de rejet. | planning_router.py:612 |
| F23 | LOGIQUE | BLOQUANT | `resequence_and_recalc()` sans savepoint/rollback. Si cascade echoue, etat partiellement modifie. | planning_router.py |
| F10 | DATA | MAJEUR | `port_stay_days` fallback a 3 jours si NULL. Incoherence possible. | escale_router.py:214 |
| F22 | DATA | MAJEUR | Pas de verification `departure_port != arrival_port`. Legs avec distance_nm=0 possibles. | planning_router.py:569-574 |
| F30 | LOGIQUE | MAJEUR | **Stowage : validation marchandises dangereuses contournable** si `batch.imo_product_class` est vide. | stowage_router.py:287-303 |
| F31 | CODE | MINEUR | **Stowage : `get_zone_capacity()` retourne None** si format palette inconnu. Division par None. | stowage_router.py:305-320 |

**Verdict Operations** : 2 BLOQUANTS, 3 MAJEURS, 1 MINEUR. Le risque principal est la creation de legs incoherents et la cascade de recalcul sans rollback.

### 3.4 Responsable Commercial (role: commercial / module: commercial + cargo)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F13 | DATA | BLOQUANT | **Dualite Order.leg_id + OrderAssignment** non resolue. Double source de verite. | order.py:76-109 |
| F25 | DATA | BLOQUANT | `create_packing_list()` ne valide pas que `order.leg_id` est non NULL. Packing lists orphelines. | cargo_router.py:98-125 |
| F14 | LOGIQUE | MAJEUR | `find_matching_leg()` retourne le premier match sans disambiguation si multiples navires. | commercial_router.py:63 |
| F15 | DATA | MAJEUR | Suppression Order ne cascade pas suppression PackingList. Donnees orphelines. | order.py, packing_list.py |
| F16 | UX | MAJEUR | Reservations passagers "draft" verrouillent cabines indefiniment (pas d'expiration). | passenger_router.py:184 |
| F32 | DATA | MAJEUR | **Race condition reference Order** : `generate_reference()` count-then-insert (L53-60). Deux ordres simultanes = meme reference. | commercial_router.py:53-60 |
| F33 | DATA | MAJEUR | **Race condition cabine passager** : check-then-insert sans lock. | passenger_router.py:184-193 |
| F34 | LOGIQUE | MINEUR | Pipedrive sync : `except: pass` silencieux. Si API fail, deal non cree sans log. | commercial_router.py:286-304 |

**Verdict Commercial** : 2 BLOQUANTS, 5 MAJEURS, 1 MINEUR. La race condition sur `generate_reference()` est un risque reel en production avec des commandes simultanees.

---

## 4. SAUVEGARDE BASE DE DONNEES

| Element | Valeur |
|---------|--------|
| Script | `scripts/backup_db.sh` (present et fonctionnel) |
| Execution | **ECHEC** — Conteneur `towt-db` non present dans cet environnement |
| Erreur | Docker daemon accessible mais conteneur absent |
| Format | pg_dump compresse (gzip) |
| Rotation | 30 jours (configurable via `BACKUP_RETENTION_DAYS`) |
| Validation structurelle | OK — script bash conforme (set -euo pipefail, .env loading, retention) |

**Action requise** : verifier que le cron est configure sur le VPS de production :
```
0 2 * * * /home/ubuntu/mytowt/scripts/backup_db.sh >> /var/log/towt-backup.log 2>&1
```

---

## 5. DOCUMENTATION

### Mises a jour effectuees
- **README.md** : Corrige compteurs architecture (12->25 modeles, 14->20 routers, 40+->91 templates)
- **README.md** : Copyright mis a jour `2025` -> `2025-2026`
- **CLAUDE.md** : Deja a jour (derniere mise a jour 29/03)

### Statistiques du projet
| Metrique | Valeur | Delta J-1 |
|----------|--------|-----------|
| Routers | 20 actifs | = |
| Modeles | 25 | = |
| Templates HTML | 91 | = |
| Lignes de code (routers) | 16 559 | = |
| Lignes de code (modeles) | 2 747 | = |
| CSS (app.css) | 1 020 lignes | = |
| Commits sur main | 91 | = |
| Branches distantes | 1 | = |

---

## 6. SYNTHESE ET PRIORITES

### Bilan securite global

| Categorie | Critique | Majeur | Moyen | Mineur |
|-----------|----------|--------|-------|--------|
| Secrets/Config | 2 (S1, S2) | 0 | 0 | 0 |
| Infra | 1 (S3) | 0 | 0 | 0 |
| Auth/Permissions | 0 | 1 (S17) | 0 | 0 |
| Transaction | 1 (S16) | 0 | 0 | 0 |
| XSS | 0 | 1 (S19) | 0 | 0 |
| Rate limiting | 0 | 1 (S18) | 0 | 0 |
| Passwords | 0 | 1 (S20) | 0 | 0 |
| Code quality | 0 | 0 | 1 (S8) | 0 |
| Path traversal | 0 | 0 | 1 (S7b) | 0 |
| Data integrity | 0 | 1 (S21) | 1 (S13) | 0 |
| File upload | 0 | 1 (S22) | 0 | 0 |
| **Total** | **4** | **6** | **3** | **0** |

### Bilan bugs fonctionnels

| Role | Bloquant | Majeur | Moyen | Mineur |
|------|----------|--------|-------|--------|
| Commandant (captain) | 0 | 4 (F2,F3,F19,F20) | 0 | 0 |
| Resp Crew (armement) | 0 | 3 (F5,F6,F28) | 1 (F29) | 1 (F7) |
| Resp Operations | 2 (F21,F23) | 3 (F10,F22,F30) | 0 | 1 (F31) |
| Resp Commercial | 2 (F13,F25) | 5 (F14,F15,F16,F32,F33) | 0 | 1 (F34) |
| **Total** | **4** | **15** | **1** | **3** |

### Actions immediates (P0 — a faire aujourd'hui)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A1 | Supprimer `await db.commit()` des 4 routers (auth, passenger_ext, pricing, tracking) | Dev | S16 |
| A2 | Ajouter validation `ETA >= ETD` en creation/edition de leg | Dev | F21 |
| A3 | Ajouter savepoint dans `resequence_and_recalc()` | Dev | F23 |
| A4 | Valider `order.leg_id is not None` avant creation packing list | Dev | F25 |

### Actions a planifier (P1 — cette semaine)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A5 | Verifier `.env` production : SECRET_KEY unique, DB password fort | DevOps | S1, S2 |
| A6 | Restreindre PostgreSQL a `127.0.0.1:5433:5432` | DevOps | S3 |
| A7 | Ajouter API key ou auth basique au tracking_router | Dev | S17 |
| A8 | Ajouter rate limiting au planning_ext_router | Dev | S18 |
| A9 | Remplacer `\|safe` par `\|tojson` pour les injections JS | Dev | S19 |
| A10 | Unifier minimum password a 8 chars dans `/my-account/password` | Dev | S20 |
| A11 | Corriger port timezone resolution : utiliser `departure_port_locode` au lieu de `departure_port_id` | Dev | F2, S21 |
| A12 | Resoudre dualite Order.leg_id / OrderAssignment | Architecte | F13 |
| A13 | Ajouter controle chevauchement affectations crew | Dev | F6 |
| A14 | Corriger path traversal claim_router : `os.path.basename()` | Dev | S7b |
| A15 | Remplacer 13 `except:` nus par `except Exception:` | Dev | S8 |
| A16 | Ajouter validation `departure_port != arrival_port` | Dev | F22 |
| A17 | Ajouter validation `embark_date <= disembark_date` pour crew | Dev | F28 |
| A18b | Appliquer `validate_file_content()` + whitelist extensions + limite taille sur crew, claim, escale, passenger_ext routers | Dev | S22 |

### Actions backlog (P2)

| # | Action | Ref |
|---|--------|-----|
| A18 | Migrer vers Alembic pour les migrations schema | Architecture |
| A19 | Logging structure (structlog) | Qualite |
| A20 | Supprimer `unsafe-inline` du CSP (nonces) | Securite |
| A21 | Tests automatises (pytest + httpx) | Qualite |
| A22 | Rate limiting Redis (multi-instance) | Infra |
| A23 | Cascade suppression Order -> PackingList | F15 |
| A24 | Expiration automatique reservations "draft" | F16 |
| A25 | Database sequence pour references Order (anti race condition) | F32 |
| A26 | SELECT FOR UPDATE pour reservation cabine | F33 |

---

## 7. EVOLUTION DEPUIS J-1

| Indicateur | J-1 (30/03) | J (31/03) | Tendance |
|------------|-------------|-----------|----------|
| Commits sur main | 91 | 91 | = |
| Vulnerabilites CRITIQUES | 4 | 4 | = |
| Vulnerabilites MAJEURES | 4 | 6 | +2 (S21, S22 nouveaux) |
| Bugs BLOQUANTS | 4 | 4 | = |
| Bugs MAJEURS | 15 | 15 | = |
| Branches stales | 0 | 0 | = |
| Correctifs appliques (securite) | 2/13 | 2/15 | +2 issues (S21, S22) |
| Correctifs appliques (fonctionnels) | 2/23 | 2/23 | = |
| Documentation | README desynchronise | README corrige | Amelioration |

### Progression globale
- **Branches** : HEAD detache resolu, master fast-forward a `f10d868`
- **Documentation** : README.md corrige (compteurs modeles/routers/templates + copyright)
- **Securite** : 2 nouvelles issues majeures identifiees (S21 — attribut `departure_port_id` inexistant dans onboard_router ; S22 — uploads sans validation d'extension/taille dans 4 routers)
- **Fonctionnel** : aucun nouveau bug ni correctif. Tous les bugs J-1 restent ouverts
- **Backup** : script valide structurellement, non executable sans Docker (a verifier sur VPS)
- **Tendance** : stabilisation du nombre de bugs, mais **0 correctif applique depuis J-1**, ce qui est preoccupant. Les 4 actions P0 doivent etre traitees en priorite

### Alerte
**Aucun correctif securite ni fonctionnel n'a ete applique depuis 3 jours.** Les issues critiques S1 (SECRET_KEY), S2 (DB password), S3 (PostgreSQL expose) et S16 (db.commit dans routes) restent ouvertes. Il est imperatif de planifier un sprint correctif cette semaine.

**Prochaine action prioritaire** : corriger les 4 actions P0 (S16, F21, F23, F25) et planifier le sprint P1 avec l'equipe.

---

*Rapport genere le 2026-03-31. Prochain audit prevu : 2026-04-01.*
