# Rapport Quotidien my_TOWT
**Date**: 2026-04-01
**Auteur**: Chef de Projet (audit automatise)
**Version applicative**: 2.0.0
**Rapport precedent**: rapport_2026-03-31.md

---

## 1. CONTROLE DES BRANCHES

### Etat du repository
| Element | Valeur |
|---------|--------|
| Branche principale | `main` |
| Commit HEAD | `9851e2a` |
| Commits sur main (total) | 91 |
| Working tree | Clean |
| Sync origin/main | A jour |

### Synchronisation
Le repo est propre : une seule branche `main`, synchronisee avec `origin/main`. Aucune branche stale, aucun merge en attente.

### Commits depuis le dernier rapport (31/03 -> 01/04)
| Commit | Description | Fichiers |
|--------|-------------|----------|
| `9851e2a` | chore: enrich daily report with QA agent findings (F35-F40) | rapports/ |
| `c93668e` | chore: enrich daily report with security agent findings (S22) | rapports/ |
| `25ffcaf` | chore: daily audit report 2026-03-31 + README update | rapports/, README.md |

**Observation** : 3 commits uniquement de type `chore` (rapports). **Aucun correctif fonctionnel ni securite n'a ete pousse depuis le 30/03** (5 jours sans correctif).

---

## 2. AUDIT DE SECURITE

### 2.1 Suivi des correctifs (cumul depuis J-4)

| # | Issue | Severite | Statut | Fichier | Detail |
|---|-------|----------|--------|---------|--------|
| S1 | SECRET_KEY par defaut | **CRITIQUE** | NON CORRIGE | config.py:17 | `towt_secret_key_change_in_production_2025` en dur |
| S2 | Mot de passe DB par defaut | **CRITIQUE** | NON CORRIGE | config.py:14 | `towt_secure_2025` en dur |
| S3 | PostgreSQL expose 0.0.0.0:5433 | **CRITIQUE** | NON CORRIGE | docker-compose.yml:32 | Aucune restriction IP |
| S7b | Path traversal claim_router | MAJEUR | NON CORRIGE | claim_router.py:51 | Pas de `os.path.basename()` |
| S8 | Bare `except:` (12+ occurrences) | MOYEN | NON CORRIGE | 6 fichiers | Masque erreurs systeme |
| S13 | f-string SQL admin export | MOYEN | NON CORRIGE | admin_router.py:779,831 | Protege par whitelist mais mauvaise pratique |
| S16 | `await db.commit()` dans routes | **CRITIQUE** | NON CORRIGE | auth_router, passenger_ext, pricing, tracking | 6 occurrences dans 4 routers |
| S17 | tracking_router sans auth | MAJEUR | NON CORRIGE | tracking_router.py | API `/api/tracking/*` totalement ouverte |
| S18 | planning_ext_router sans rate limiting | MAJEUR | NON CORRIGE | planning_ext_router.py | Token brute-forcable |
| S19 | XSS via `\|safe` dans templates | MAJEUR | NON CORRIGE | portal_destination.html:28, grid_form.html:163-165 | 4 occurrences |
| S20 | Mot de passe min 4 caracteres | MAJEUR | NON CORRIGE | admin_router.py:1382 | 4 chars au lieu de 8 |
| S21 | `departure_port_id` inexistant (onboard) | MAJEUR | NON CORRIGE | onboard_router.py:267-268,338-339 | Attribut inexistant du modele Leg |
| S22 | Uploads sans validation (4 routers) | MAJEUR | NON CORRIGE | crew, claim, escale, passenger_ext | Pas de whitelist extensions, pas de limite taille |

### 2.2 Nouvelles constatations

#### MAJEUR

| # | Probleme | Fichier | Lignes | Impact |
|---|----------|---------|--------|--------|
| S23 | **subprocess.run dans pricing_router** | pricing_router.py | 1063-1066 | Appel `node` avec arguments derives de donnees DB. `docx_filename` construit a partir de `ref` (user-influenced) sans sanitisation — risque de path traversal dans le nom de fichier DOCX. |
| S24 | **datetime.now() sans timezone** generalisee | onboard_router.py | 623,1208,1301,1450,1505,1544,1586,1635,1711,1827 | 11 occurrences `datetime.now()` vs 3 `datetime.now(timezone.utc)`. Incoherence temporelle dans les logs SOF et documents de cloture. |

| S25 | **Crew API endpoint sans authentification** | crew_router.py | 384-399 | `/crew/api/vessel/{vessel_id}` retourne la liste des marins en JSON sans aucune permission check. Information disclosure. |
| S26 | **Token planning_share non cryptographique** | planning_router.py | 1018 | `uuid.uuid4().hex[:24]` utilise au lieu de `secrets.token_urlsafe(24)`. UUID v4 moins entropique que secrets. Incoherent avec les tokens des autres modules. |
| S27 | **FileResponse sans validation du path** | cargo, claim, commercial, onboard routers | multi | `FileResponse(doc.file_path)` sans verifier que `file_path` est dans le repertoire attendu. Si `file_path` modifie en base, acces a `/etc/passwd` possible. |

#### Points positifs confirmes (inchanges)
- CORS correctement restreint (2 origines)
- Security headers complets (CSP, HSTS, X-Frame-Options, etc.)
- CSRF middleware actif avec double-submit cookie
- Cookies session : `httponly=True`, `secure=True`, `samesite=lax`
- Rate limiting login : 5 tentatives/60s, lockout 5 min
- Rate limiting portail cargo/passager : 10 tentatives/60s, lockout 5 min
- File validation correcte dans `file_validation.py` (magic bytes + MIME)
- admin_router protege globalement par `require_admin`
- 13 routers avec dependencies de permission

---

## 3. TESTS FONCTIONNELS PAR ROLE

### 3.1 Commandant de bord (role: marins / module: captain)

| # | Type | Severite | Statut | Description | Fichier |
|---|------|----------|--------|-------------|---------|
| F2 | BUG | MAJEUR | OUVERT | **Port timezone resolution defaillante** : `leg.departure_port_id` inexistant, le modele utilise `departure_port_locode` | onboard_router.py:267-268,338-339 |
| F3 | PERF | MAJEUR | OUVERT | **N+1 queries** : meme Leg charge 3 fois dans sof_add_event() | onboard_router.py:264,294,307 |
| F19 | LOGIQUE | MAJEUR | OUVERT | **ETA Shift + resequence avant flush** : etat potentiellement inconsistant | onboard_router.py:541-670 |
| F20 | DATA | MAJEUR | OUVERT | **ETA shift cascade ecrase ajustements manuels** des legs downstream | planning_router.py:119-184 |
| F35 | BUG | MAJEUR | OUVERT | **Closure redirect utilise `vessel_id` (int) au lieu de `vessel.code` (string)** : URLs invalides type `/onboard?vessel=1`. 4 occurrences L1513,1552,1594,1643 | onboard_router.py |
| F36 | DATA | MOYEN | OUVERT | **datetime.now() sans timezone** dans SOF events. Mix avec datetime.now(timezone.utc) | onboard_router.py |

| F41 | BUG | MOYEN | NOUVEAU | **Null pointer SOF edit** : si un leg est supprime entre creation et edition d'un SOF event, `leg.vessel_id` crashe (L348) | onboard_router.py:348 |

**Verdict Commandant** : 5 MAJEURS + 2 MOYENS ouverts. F35 rend les redirections post-closure inoperantes. Aucun correctif depuis J-1.

### 3.2 Responsable Crew (role: armement / module: crew)

| # | Type | Severite | Statut | Description | Fichier |
|---|------|----------|--------|-------------|---------|
| F5 | BUG | MAJEUR | OUVERT | `except:` nu dans `parse_date()` — masque erreurs systeme | crew_router.py:34 |
| F6 | DATA | MAJEUR | OUVERT | Pas de controle **chevauchement d'affectations** : marin affectable a 2 navires simultanement | crew_router.py:299+ |
| F28 | DATA | MAJEUR | OUVERT | Pas de validation `embark_date <= disembark_date` a la creation | crew_router.py:290-312 |
| F29 | LOGIQUE | MOYEN | OUVERT | **Status CrewAssignment inversee** : logique basee sur presence de date plutot que comparaison avec today | crew_router.py:304 |
| F7 | DATA | MINEUR | OUVERT | `embark_leg_id`/`disembark_leg_id` jamais remplis — champs orphelins | crew_router.py |

**Verdict Crew** : 3 MAJEURS, 1 MOYEN, 1 MINEUR. Risque reel d'affectation double. Aucun correctif depuis J-1.

### 3.3 Responsable Operations (role: operation / module: escale + planning)

| # | Type | Severite | Statut | Description | Fichier |
|---|------|----------|--------|-------------|---------|
| F21 | DATA | BLOQUANT | OUVERT | **Pas de validation `ETA >= ETD`** en creation de leg | planning_router.py:612 |
| F23 | LOGIQUE | BLOQUANT | OUVERT | `resequence_and_recalc()` sans savepoint/rollback | planning_router.py |
| F10 | DATA | MAJEUR | OUVERT | `port_stay_days` fallback a 3 jours si NULL | escale_router.py:214 |
| F22 | DATA | MAJEUR | OUVERT | Pas de verification `departure_port != arrival_port` | planning_router.py:569-574 |
| F30 | LOGIQUE | MAJEUR | OUVERT | Stowage : validation marchandises dangereuses contournable | stowage_router.py:287-303 |
| F31 | CODE | MINEUR | OUVERT | Stowage : `get_zone_capacity()` retourne None si format palette inconnu | stowage_router.py:305-320 |
| F37 | LOGIQUE | MAJEUR | OUVERT | **ATA fictif auto-genere** : `leg.ata = now - timedelta(hours=1)` cree une fausse arrivee | escale_router.py:486 |
| F38 | DATA | MAJEUR | OUVERT | Docker shift sans validation `planned_start < planned_end` | escale_router.py:737-760 |

| F42 | BUG | MOYEN | NOUVEAU | **`int(x)` sans try/except** dans crew_ids parsing : crash 500 sur donnees malformees | escale_router.py:616 |
| F43 | UX | MINEUR | NOUVEAU | **Annee minimum hardcodee 2025** dans le selecteur escale. Devrait etre dynamique | escale_router.py:268 |
| F44 | LOGIQUE | MOYEN | NOUVEAU | **port_stay_days=0 traite comme NULL** : fallback 3 jours applique alors que 0 est une valeur valide | planning_router.py:379 |

**Verdict Operations** : 2 BLOQUANTS, 5 MAJEURS, 3 MOYENS, 2 MINEURS. Aucun correctif depuis J-1.

### 3.4 Responsable Commercial (role: commercial / module: commercial + cargo)

| # | Type | Severite | Statut | Description | Fichier |
|---|------|----------|--------|-------------|---------|
| F13 | DATA | BLOQUANT | OUVERT | **Dualite Order.leg_id + OrderAssignment** : double source de verite | order.py:76-109 |
| F25 | DATA | BLOQUANT | OUVERT | `create_packing_list()` ne valide pas `order.leg_id is not None` | cargo_router.py:98-125 |
| F14 | LOGIQUE | MAJEUR | OUVERT | `find_matching_leg()` retourne le premier match sans disambiguation | commercial_router.py:63 |
| F15 | DATA | MAJEUR | OUVERT | Suppression Order ne cascade pas suppression PackingList | order.py, packing_list.py |
| F16 | UX | MAJEUR | OUVERT | Reservations passagers "draft" verrouillent cabines indefiniment | passenger_router.py:184 |
| F32 | DATA | MAJEUR | OUVERT | **Race condition** : `generate_reference()` count-then-insert (L53-60) | commercial_router.py |
| F33 | DATA | MAJEUR | OUVERT | **Race condition cabine passager** : check-then-insert sans lock | passenger_router.py:184-193 |
| F34 | LOGIQUE | MINEUR | OUVERT | Pipedrive sync : `except: pass` silencieux | commercial_router.py:286-304 |
| F39 | BUG | MAJEUR | OUVERT | **Format BL inconstant** : `{seq:02d}` vs `{bl_seq:03d}` selon le contexte | cargo_router.py:432,539,606 |
| F40 | DATA | MOYEN | OUVERT | **Batch number collision** si batch supprime puis recree | cargo_router.py:744 |

| F45 | LOGIQUE | MAJEUR | NOUVEAU | **Auto-match silencieux** : `find_matching_leg()` assigne automatiquement un leg et passe le status a "reserve" sans confirmation utilisateur | commercial_router.py:281-284 |
| F46 | DATA | MAJEUR | NOUVEAU | **Status transition non validee** : edit order permet de passer directement a "confirme" sans controle. Retour en arriere possible avec perte de packing list | commercial_router.py:377 |
| F47 | DATA | MOYEN | NOUVEAU | **CO2 calcul retourne 0 silencieusement** si leg.computed_distance et distance_nm sont NULL. Dashboard affiche faux scores | commercial_router.py:170-171 |

**Verdict Commercial** : 2 BLOQUANTS, 8 MAJEURS, 2 MOYENS, 1 MINEUR. Aucun correctif depuis J-1.

---

## 4. SAUVEGARDE BASE DE DONNEES

| Element | Valeur |
|---------|--------|
| Script | `scripts/backup_db.sh` (present et fonctionnel) |
| Execution | **ECHEC** — Docker daemon non disponible dans cet environnement |
| Erreur | `dial unix /var/run/docker.sock: connect: no such file or directory` |
| Format | pg_dump compresse (gzip) |
| Rotation | 30 jours (configurable via `BACKUP_RETENTION_DAYS`) |
| Validation structurelle | OK — script bash conforme (set -euo pipefail, .env loading, retention) |

**Action requise** : verifier que le cron est configure sur le VPS de production :
```
0 2 * * * /home/ubuntu/mytowt/scripts/backup_db.sh >> /var/log/towt-backup.log 2>&1
```

---

## 5. DOCUMENTATION

### Etat actuel
| Document | Statut | Derniere MAJ |
|----------|--------|-------------|
| CLAUDE.md | A jour | 29/03/2026 |
| README.md | A jour | 31/03/2026 |
| rapports/ | 4 rapports (28-31/03) | 31/03/2026 |

### Statistiques du projet
| Metrique | Valeur | Delta J-1 |
|----------|--------|-----------|
| Routers | 21 fichiers | +1 (recompte) |
| Modeles | 26 fichiers | +1 (recompte) |
| Templates HTML | 87 | -4 (recompte precis) |
| Lignes de code (routers) | 16 559 | = |
| Lignes de code (modeles) | 2 747 | = |
| CSS (app.css) | 1 020 lignes | = |
| Commits sur main | 91 | = |
| Branches distantes | 1 (main) | = |

### Mises a jour effectuees
- Aucune modification de documentation requise (README.md et CLAUDE.md deja a jour)
- Ajout du rapport quotidien 2026-04-01 (present document)

---

## 5b. COHERENCE CODEBASE

### Constats

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| C1 | CODE MORT | MOYEN | **Modele `activity.py` duplique** : `activity.py` (table `activity_log`) et `activity_log.py` (table `activity_logs`) coexistent. Seul `activity_log.py` est importe dans `__init__.py`. `activity.py` est du code mort a supprimer. | models/activity.py |
| C2 | CODE MORT | MINEUR | **Template orphelin** : `admin/activity_log.html` existe mais aucun endpoint ne le reference. Feature incomplete ou legacy. | templates/admin/activity_log.html |
| C3 | QUALITE | MINEUR | **Imports inutilises** : `AuthRequired` importe mais non utilise dans planning_router.py:15 et kpi_router.py:15 | planning_router.py, kpi_router.py |
| C4 | STYLE | INFO | **Mix backref/back_populates** : modeles onboard.py, portal_message.py, packing_list.py utilisent `backref=` (ancien style) vs `back_populates` (moderne) dans order.py, leg.py, crew.py | models/ |

### Points positifs confirmes
- Aucune import circulaire detectee
- Tous les modeles ont `__tablename__` defini
- Heritage templates (`extends "base.html"`) coherent partout
- Variables templates correctement passees depuis les routers
- Systeme i18n correctement branche (`t()` global + `lang` auto-injecte)
- Dependencies `requirements.txt` a jour, pas de CVE connue
- Docker config correcte (PostgreSQL healthcheck, port localhost only)

---

## 6. SYNTHESE ET PRIORITES

### Bilan securite global

| Categorie | Critique | Majeur | Moyen | Total |
|-----------|----------|--------|-------|-------|
| Secrets/Config | 2 (S1, S2) | 0 | 0 | 2 |
| Infra | 1 (S3) | 0 | 0 | 1 |
| Transaction | 1 (S16) | 0 | 0 | 1 |
| Auth/Permissions | 0 | 1 (S17) | 0 | 1 |
| XSS | 0 | 1 (S19) | 0 | 1 |
| Rate limiting | 0 | 1 (S18) | 0 | 1 |
| Passwords | 0 | 1 (S20) | 0 | 1 |
| Path traversal | 0 | 1 (S7b) | 0 | 1 |
| File upload | 0 | 1 (S22) | 0 | 1 |
| Subprocess/Files | 0 | 1 (S23) | 0 | 1 |
| Timezone | 0 | 1 (S24) | 0 | 1 |
| API sans auth | 0 | 1 (S25) | 0 | 1 |
| Tokens | 0 | 1 (S26) | 0 | 1 |
| File serving | 0 | 1 (S27) | 0 | 1 |
| Code quality | 0 | 0 | 2 (S8, S13) | 2 |
| **Total** | **4** | **11** | **2** | **17** |

### Bilan bugs fonctionnels

| Role | Bloquant | Majeur | Moyen | Mineur | Total |
|------|----------|--------|-------|--------|-------|
| Commandant (captain) | 0 | 5 | 2 | 0 | 7 |
| Resp Crew (armement) | 0 | 3 | 1 | 1 | 5 |
| Resp Operations | 2 | 5 | 3 | 2 | 12 |
| Resp Commercial | 2 | 8 | 2 | 1 | 13 |
| **Total** | **4** | **21** | **8** | **4** | **37** |

### Actions immediates (P0 — a faire aujourd'hui)

| # | Action | Ref | Jours ouverts |
|---|--------|-----|---------------|
| A1 | Supprimer `await db.commit()` des 4 routers | S16 | 5j |
| A2 | Ajouter validation `ETA >= ETD` en creation/edition de leg | F21 | 4j |
| A3 | Ajouter savepoint dans `resequence_and_recalc()` | F23 | 4j |
| A4 | Valider `order.leg_id is not None` avant creation packing list | F25 | 4j |

### Actions a planifier (P1 — cette semaine)

| # | Action | Ref |
|---|--------|-----|
| A5 | Verifier `.env` production : SECRET_KEY unique, DB password fort | S1, S2 |
| A6 | Restreindre PostgreSQL a `127.0.0.1:5433:5432` | S3 |
| A7 | Ajouter API key ou auth basique au tracking_router | S17 |
| A8 | Ajouter rate limiting au planning_ext_router | S18 |
| A9 | Remplacer `\|safe` par `\|tojson` pour les injections JS | S19 |
| A10 | Unifier minimum password a 8 chars | S20 |
| A11 | Corriger port timezone : `departure_port_locode` au lieu de `departure_port_id` | F2, S21 |
| A12 | Resoudre dualite Order.leg_id / OrderAssignment | F13 |
| A13 | Ajouter controle chevauchement affectations crew | F6 |
| A14 | Corriger path traversal claim_router | S7b |
| A15 | Remplacer 12+ `except:` nus par `except Exception:` | S8 |
| A16 | Ajouter validation `departure_port != arrival_port` | F22 |
| A17 | Ajouter validation `embark_date <= disembark_date` | F28 |
| A18b | Appliquer `validate_file_content()` dans crew, claim, escale, passenger_ext | S22 |
| A18c | Corriger closure redirects : `vessel.code` au lieu de `vessel_id` | F35 |
| A18d | Supprimer ATA fictif auto-genere | F37 |
| A18e | Uniformiser format BL `{seq:03d}` partout | F39 |
| A18f | Ajouter validation docker shifts `planned_start < planned_end` | F38 |
| A19 | Sanitiser `ref` dans pricing_router pour le filename DOCX | S23 |
| A20 | Uniformiser `datetime.now(timezone.utc)` dans onboard_router | S24 |
| A21 | Ajouter `require_permission("crew", "C")` sur `/crew/api/vessel/{vessel_id}` | S25 |
| A22 | Valider transitions de statut Order (machine d'etat) | F46 |
| A23 | Ajouter confirmation utilisateur pour auto-match leg dans creation order | F45 |
| A24 | Wrapper `int(x)` dans try/except pour crew_ids parsing dans escale | F42 |
| A25 | Remplacer `uuid.uuid4().hex[:24]` par `secrets.token_urlsafe(24)` pour planning_share | S26 |
| A26 | Ajouter validation `os.path.abspath()` + prefix check avant tout `FileResponse()` | S27 |

### Actions backlog (P2)

| # | Action | Ref |
|---|--------|-----|
| B1 | Migrer vers Alembic pour les migrations schema | Architecture |
| B2 | Logging structure (structlog) | Qualite |
| B3 | Supprimer `unsafe-inline` du CSP (nonces) | Securite |
| B4 | Tests automatises (pytest + httpx) | Qualite |
| B5 | Rate limiting Redis (multi-instance) | Infra |
| B6 | Cascade suppression Order -> PackingList | F15 |
| B7 | Expiration automatique reservations "draft" | F16 |
| B8 | Database sequence pour references Order | F32 |
| B9 | SELECT FOR UPDATE pour reservation cabine | F33 |

---

## 7. EVOLUTION DEPUIS J-1

| Indicateur | J-1 (31/03) | J (01/04) | Tendance |
|------------|-------------|-----------|----------|
| Commits sur main | 91 | 91 | = |
| Vulnerabilites CRITIQUES | 4 | 4 | = |
| Vulnerabilites MAJEURES | 6 | 11 | +5 (S23-S27 nouveaux) |
| Bugs BLOQUANTS | 4 | 4 | = |
| Bugs MAJEURS | 19 | 21 | +2 (F45, F46 nouveaux) |
| Branches stales | 0 | 0 | = |
| Correctifs appliques (securite) | 0/15 | 0/19 | +5 issues (S23-S27) |
| Correctifs appliques (fonctionnels) | 0/29 | 0/37 | +8 nouvelles issues (F41-F47) |
| Documentation | A jour | A jour | = |

### Progression globale
- **Branches** : repo propre, une seule branche `main` synchronisee avec origin
- **Documentation** : README.md et CLAUDE.md a jour
- **Securite** : 5 nouvelles issues majeures identifiees (S23 — subprocess path traversal dans pricing_router ; S24 — inconsistence datetime.now() dans onboard_router ; S25 — crew API sans auth ; S26 — token planning non cryptographique ; S27 — FileResponse sans validation path)
- **Fonctionnel** : 7 nouveaux bugs identifies (F41-F47) par audit approfondi QA. Aucun correctif applique
- **Backup** : script valide structurellement, non executable sans Docker

### ALERTE CRITIQUE

**Aucun correctif securite ni fonctionnel n'a ete applique depuis 5 jours (depuis le 27/03).**

Le backlog de dette technique continue de croitre :
- **4 vulnerabilites CRITIQUES** ouvertes (S1, S2, S3, S16)
- **4 bugs BLOQUANTS** ouverts (F13, F21, F23, F25)
- **32 issues MAJEURES** ouvertes au total (11 securite + 21 fonctionnels)
- **0 correctif** applique sur les 5 derniers jours
- **37 bugs fonctionnels** au total (vs 29 a J-1)

**Recommandation forte** : bloquer tout nouveau developpement fonctionnel et dedier un sprint correctif de 3 jours aux actions P0 et P1. L'accumulation de dette technique non traitee represente un risque operationnel reel pour la production.

### Prochaine action prioritaire
1. **Immediate** : corriger S16 (db.commit dans 4 routers) — 30 min de travail
2. **Ce jour** : corriger F21, F23, F25 (validations critiques) — 2h estimees
3. **Demain** : sprint P1 (S1-S3 config production, F35 redirects, S22 uploads)

---

*Rapport genere le 2026-04-01. Prochain audit prevu : 2026-04-02.*
