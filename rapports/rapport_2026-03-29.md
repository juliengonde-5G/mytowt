# Rapport Quotidien my_TOWT
**Date**: 2026-03-29
**Auteur**: Chef de Projet (audit automatise)
**Version applicative**: 2.0.0
**Rapport precedent**: rapport_2026-03-28.md

---

## 1. CONTROLE DES BRANCHES

### Etat du repository
| Element | Valeur |
|---------|--------|
| Branche principale | `main` (origin) / `master` (local) |
| Commit HEAD | `0f13f54` |
| Commits sur main (total) | 91 |
| Working tree | Clean |

### Synchronisation effectuee
6 commits etaient en avance (detached HEAD) et ont ete merges (fast-forward) sur master :

| Commit | Description | Fichiers |
|--------|-------------|----------|
| `37a1779` | fix: remove duplicate local Claim import (KPI) | kpi_router.py |
| `fe633b4` | fix: KPI page NameError - totals -> env_totals | kpi_router.py |
| `f98b528` | fix: KPI modal + BL numbers unique per leg | kpi/index.html, cargo_router.py |
| `1e168b3` | fix: passenger_bookings.leg_id nullable | passenger.py |
| `5b6d9a0` | chore: daily audit report + backup script | rapports/, scripts/ |
| `0f13f54` | chore: update daily report | rapports/ |

**Statut** : `master` et `origin/main` synchronises a `0f13f54`.

### Branches distantes
| Branche | Statut | Commentaire |
|---------|--------|-------------|
| `origin/main` | A jour | Synchronisee |
| `origin/claude/OnStage-0Ldyk` | STALE (vide) | Aucun commit en avance - a supprimer |
| `origin/claude/add-port-country-filter-0Ldyk` | STALE (vide) | Aucun commit en avance - a supprimer |
| `origin/claude/consolidate-main` | STALE (vide) | Deja merge - a supprimer |
| `origin/claude/develop-chat-app-275SR` | STALE (80+ commits) | Pre-consolidation, deja integre via merge - a supprimer |
| `origin/claude/fix-stopover-creation-bug-HVTEJ` | STALE (80+ commits) | Pre-consolidation, deja integre via merge - a supprimer |
| `origin/claude/optimize-loading-plan-XXwVM` | STALE (1 commit) | Pre-consolidation - a supprimer |
| `origin/claude/update-bol-template-TPrpS` | STALE (7 commits) | Pre-consolidation - a supprimer |

**Recommandation** : supprimer les 7 branches stales pour nettoyer le repo.
```bash
git push origin --delete claude/OnStage-0Ldyk claude/add-port-country-filter-0Ldyk claude/consolidate-main claude/develop-chat-app-275SR claude/fix-stopover-creation-bug-HVTEJ claude/optimize-loading-plan-XXwVM claude/update-bol-template-TPrpS
```

---

## 2. AUDIT DE SECURITE

### 2.1 Suivi des correctifs (rapport J-1)

| Issue | Statut | Commentaire |
|-------|--------|-------------|
| S1 - SECRET_KEY par defaut | NON CORRIGE | `config.py:17` - Toujours `towt_secret_key_change_in_production_2025` |
| S2 - Mot de passe DB par defaut | NON CORRIGE | `config.py:14`, `docker-compose.yml:8,27` |
| S3 - Port PostgreSQL expose 0.0.0.0:5433 | NON CORRIGE | `docker-compose.yml:32` |
| S4 - Uploads sans validation | PARTIELLEMENT CORRIGE | onboard_router OK, mais passenger_ext, crew, escale, claim toujours non valides |
| S5 - onboard_router sans permissions | NON CORRIGE | 0 occurrences de `require_permission` (27 routes POST/DELETE) |
| S6 - stowage/tracking sans auth router | NON CORRIGE | `main.py:207,209` |
| S7b - Path traversal claim_router | NON CORRIGE | `claim_router.py:51` - pas de `os.path.basename()` |
| S8 - Bare `except:` | NON CORRIGE | 13 occurrences dans 5 fichiers |
| F9 - Cascade resequence_and_recalc | CORRIGE | Logique downstream OK |
| F13 - Dualite Order.leg_id/OrderAssignment | NON CORRIGE | Architectural |

**Bilan** : 1/10 correctifs appliques, 1 partiel. **8 issues en souffrance dont 4 CRITIQUES.**

### 2.2 Nouvelles constatations

#### CRITIQUE

| # | Probleme | Fichier | Impact |
|---|----------|---------|--------|
| S13 | **f-string SQL dans admin export** | `admin_router.py:779,831` | `text(f'SELECT * FROM "{table_name}"')` — bien que protege par ALLOWED_TABLES, utilise f-string au lieu de binding. Risque si whitelist contournee. |

#### MAJEUR

| # | Probleme | Fichier | Impact |
|---|----------|---------|--------|
| S14 | **Bare except: dans commercial_router** (nouveau) | `commercial_router.py:40,45,50` | 3 `except:` nus dans helpers `pf()`, `pi()`, `pd()` — masquent les erreurs systeme |
| S15 | **Bare except: dans admin_router** | `admin_router.py:723` | `except: return str(val)` dans helper de formatage |

#### Points positifs confirmes
- CORS correctement restreint (my.towt.eu + IP VPS)
- Security headers complets (CSP, HSTS, X-Frame-Options, etc.)
- CSRF middleware actif avec double-submit cookie pattern
- Cookies session : `httponly=True`, `secure=True`, `samesite=lax`
- Rate limiting login : 5 tentatives/60s, lockout 5 min
- Rate limiting portail : 10 tentatives/60s, lockout 5 min
- File validation correcte dans `file_validation.py` (magic bytes)
- Dependencies a jour, pas de CVE connue
- Tokens portail valides avec expiration + logging

---

## 3. TESTS FONCTIONNELS PAR ROLE

### 3.1 Commandant de bord (role: marins / module: captain)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F1 | BUG | BLOQUANT | **Aucune permission granulaire** : 27 routes POST/DELETE dans onboard_router utilisent uniquement `get_current_user`. Tout utilisateur authentifie peut modifier SOF, ATA/ATD, fermer une escale, uploader des documents. | `onboard_router.py` (toutes les routes) |
| F2 | BUG | MAJEUR | **Redirection ATA/ATD si vessel_id NULL** : URL malformee `/onboard?vessel=&leg_id=...` si le vessel_id n'est pas renseigne. | `onboard_router.py:468` |
| F3 | UX | MINEUR | Messages d'erreur ATA/ATD generiques ("Format de date invalide") sans preciser le format attendu (ISO 8601). | `onboard_router.py:431` |
| F4 | UX | MINEUR | Import `validate_file_content` present mais non applique uniformement sur tous les endpoints upload. | `onboard_router.py` |

### 3.2 Responsable Crew (role: armement / module: crew)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F5 | BUG | MAJEUR | `except:` nu dans `parse_date()` — masque les erreurs systeme (KeyboardInterrupt, SystemExit). | `crew_router.py:34` |
| F6 | DATA | MAJEUR | Pas de controle de chevauchement d'affectations : un marin peut etre affecte a 2 navires simultanement sur des periodes qui se recoupent. | `crew_router.py:299+` |
| F7 | DATA | MINEUR | `embark_leg_id` / `disembark_leg_id` jamais remplis par le formulaire d'affectation — champs orphelins. | `crew_router.py` |
| F8 | UX | COSMÉTIQUE | Doublons potentiels dans les alertes passeport/visa si les deux expirent le meme jour. | `crew_router.py:111` |

### 3.3 Responsable Operations (role: operation / module: escale + planning)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F9 | LOGIQUE | CORRIGE | `resequence_and_recalc()` : logique de cascade downstream maintenant correcte. | `planning_router.py:119-184` |
| F10 | DATA | MAJEUR | `port_stay_days` : fallback a 3 jours si NULL en code, mais `default=3` en SQLAlchemy. Incoherence si valeur mise a NULL directement en base. | `escale_router.py:214` |
| F11 | LOGIQUE | MAJEUR | `update_finance_actual_duration()` ne recalcule que le leg courant, pas les legs suivants. Finance desynchronisee si escale prolongee. | `escale_router.py:160` |
| F12 | UX | MINEUR | `compute_port_status()` : retourne "pilote_arrivee" meme pour un leg "completed" si ATA et ATD sont NULL. | `escale_router.py:143` |

### 3.4 Responsable Commercial (role: commercial / module: commercial + cargo)

| # | Type | Severite | Description | Fichier |
|---|------|----------|-------------|---------|
| F13 | DATA | BLOQUANT | **Dualite Order.leg_id + OrderAssignment** non resolue. Double source de verite pour l'affectation d'un ordre a un leg. Risque de desynchronisation. | `order.py:76-109` |
| F14 | LOGIQUE | MAJEUR | `find_matching_leg()` retourne le premier leg correspondant sans disambiguation si plusieurs navires ont le meme itineraire. | `commercial_router.py:63` |
| F15 | DATA | MAJEUR | Suppression d'un order ne cascade pas la suppression du PackingList associe. Donnees orphelines possibles. | `order.py`, `packing_list.py` |
| F16 | UX | MAJEUR | Reservations passagers "draft" verrouillent les cabines indefiniment, sans mecanisme d'expiration automatique. | `passenger_router.py:184` |
| F17 | LOGIQUE | MINEUR | Pipedrive : recherche client par nom avec `.limit(1)` — si doublons, mauvais client selectionne. | `commercial_router.py:295` |
| F18 | UX | MINEUR | Validation IMDG (marchandises dangereuses) genere des warnings mais n'empeche pas l'approbation du packing list. | `cargo_router.py:191` |

---

## 4. SAUVEGARDE BASE DE DONNEES

| Element | Valeur |
|---------|--------|
| Script | `scripts/backup_db.sh` (present et fonctionnel) |
| Execution | **ECHEC** — Docker non disponible dans cet environnement |
| Erreur | `failed to connect to docker API at unix:///var/run/docker.sock` |
| Format | pg_dump compresse (gzip) |
| Rotation | 30 jours (configurable) |

**Action requise** : verifier que le cron est configure sur le VPS de production :
```
0 2 * * * /home/ubuntu/mytowt/scripts/backup_db.sh >> /var/log/towt-backup.log 2>&1
```

---

## 5. DOCUMENTATION

### Mises a jour effectuees
- **CLAUDE.md** : ajout de 5 modeles manquants dans la structure du projet :
  - `activity.py` (user activity tracking)
  - `emission_parameter.py` (CO2 emission factors)
  - `notification.py` (system notifications)
  - `portal_message.py` (client portal messaging)
  - `portal_access_log.py` (portal token access audit)

### Statistiques du projet
| Metrique | Valeur |
|----------|--------|
| Routers | 21 (dont 20 actifs + `__init__.py` vide) |
| Modeles | 26 |
| Templates HTML | 91 |
| Lignes de code (routers) | 16 559 |
| Lignes de code (modeles) | 2 747 |
| CSS (app.css) | 1 020 lignes |
| Commits sur main | 91 |
| Branches distantes | 8 (dont 7 stales) |

---

## 6. SYNTHESE ET PRIORITES

### Bilan securite global

| Categorie | Critique | Majeur | Mineur | Info |
|-----------|----------|--------|--------|------|
| Injection SQL | 1 (S13) | 0 | 0 | 0 |
| Auth/Permissions | 0 | 1 (S5 reconduit) | 0 | 0 |
| Uploads | 0 | 1 (S4 partiel) | 0 | 0 |
| Path traversal | 1 (S7b reconduit) | 0 | 0 | 0 |
| Secrets | 2 (S1, S2 reconduits) | 0 | 0 | 0 |
| Infra | 1 (S3 reconduit) | 0 | 0 | 0 |
| Code quality | 0 | 2 (S14, S15) | 2 | 0 |
| **Total** | **5** | **4** | **2** | **0** |

### Bilan bugs fonctionnels

| Role | Bloquant | Majeur | Mineur | Cosmetique |
|------|----------|--------|--------|------------|
| Commandant (captain) | 1 | 1 | 2 | 0 |
| Resp Crew (armement) | 0 | 2 | 1 | 1 |
| Resp Operations | 0 | 2 | 1 | 0 |
| Resp Commercial | 1 | 3 | 2 | 0 |
| **Total** | **2** | **8** | **6** | **1** |

### Actions immediates (P0 — a faire aujourd'hui)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A1 | Restreindre PostgreSQL a `127.0.0.1:5433:5432` | DevOps | S3 |
| A2 | Verifier `.env` production : SECRET_KEY unique, DB password fort | DevOps | S1, S2 |
| A3 | Ajouter `require_permission("captain", "M"/"S")` aux 27 routes onboard | Dev | S5, F1 |
| A4 | Corriger path traversal claim_router : `os.path.basename()` | Dev | S7b |
| A5 | Ajouter file validation (taille + extension) sur passenger_ext, crew, escale, claim | Dev | S4 |

### Actions a planifier (P1 — cette semaine)

| # | Action | Responsable | Ref |
|---|--------|-------------|-----|
| A6 | Resoudre dualite Order.leg_id / OrderAssignment | Architecte | F13 |
| A7 | Ajouter permissions stowage_router et tracking_router | Dev | S6 |
| A8 | Remplacer 13 `except:` nus par `except Exception:` | Dev | S8, S14, S15 |
| A9 | Ajouter controle chevauchement affectations crew | Dev | F6 |
| A10 | Cascade suppression Order -> PackingList | Dev | F15 |
| A11 | Ajouter expiration automatique reservations "draft" | Dev | F16 |
| A12 | Supprimer 7 branches stales | DevOps | Branches |

### Actions backlog (P2)

| # | Action | Ref |
|---|--------|-----|
| A13 | Migrer vers Alembic pour les migrations schema | S12 |
| A14 | Logging structure (structlog) | S11 |
| A15 | Supprimer `unsafe-inline` du CSP (nonces) | S7 |
| A16 | Tests automatises (pytest + httpx) | - |
| A17 | Rate limiting Redis (multi-instance) | S10 |
| A18 | Disambiguation `find_matching_leg()` | F14 |

---

## 7. EVOLUTION DEPUIS J-1

| Indicateur | J-1 (28/03) | J (29/03) | Tendance |
|------------|-------------|-----------|----------|
| Commits sur main | 85 | 91 | +6 |
| Vulnerabilites CRITIQUES | 4 | 5 | +1 (S13 nouveau) |
| Vulnerabilites MAJEURES | 3 | 4 | +1 (S14, S15 identifies) |
| Bugs fonctionnels | 18 | 17 | -1 (F9 corrige) |
| Correctifs appliques | 0/10 | 1/10 | +1 (F9) |
| Branches stales | 0 | 7 | +7 (nouvelles branches identifiees) |
| Modeles documentes | 20/20 | 26/26 | +6 (5 manquants ajoutes) |

**Prochaine action prioritaire** : corriger les 5 issues P0 avant le prochain deploiement.

---

*Rapport genere le 2026-03-29. Prochain audit prevu : 2026-03-30.*
